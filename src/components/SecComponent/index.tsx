import React from 'react'
import { View } from 'react-native'
import SecIcon from '@assets/secIcon'
import styles from './styles'

export default function SecComponent() {
  return (
    <View style={styles.container}>
      <SecIcon />
    </View>
  )
}