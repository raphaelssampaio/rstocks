import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  inputContainer: {
    flexDirection: 'row',
    borderRadius: 24,
    borderColor: '#3500D4',
    borderWidth: 2,
    alignItems: 'center'
  }
})